import sqlite3
conn = sqlite3.connect("rc3.db")
c = conn.cursor()
def tableCreate():
    #this can only be done once
    c.execute("CREATE TABLE stuffToPlot (PGC_number TEXT,ra REAL, dec REAL, in_SDSS_footprint BOOLEAN ) ") # create table and specify column, primary key is auto incremnting ID numbe

def dataEntry():
    c.execute("INSERT INTO stuffToPlot VALUES( 1,'2014-04-14','keyword1',100)")
    conn.commit()
